import 'package:flutter/material.dart';

class ConfiguePage extends StatelessWidget {
  const ConfiguePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}